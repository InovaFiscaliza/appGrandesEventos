"""
Componente de cabeçalho reutilizável.

Funções a migrar de abordagem.py:
  - _img_b64(path)   → converte imagem para base64
  - render_header()  → renderiza logo + título do app no topo de cada tela
"""

# TODO: migrar funções acima de abordagem.py para cá
